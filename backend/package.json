{
  "name": "crash-demo-backend",
  "version": "0.1.0",
  "description": "Demo (virtual-currency only) crash game backend for a Telegram Mini App. NOT for real-money wagering — see README.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "db:init": "node src/db.js --init"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "socket.io": "^4.7.5"
  }
}
